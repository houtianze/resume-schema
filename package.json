{
  "name": "resume-schema-ibic",
  "version": "0.0.18",
  "description": "JSON Resume Schema (fork to support optional date)",
  "main": "validator.js",
  "scripts": {
    "test": "tape **/*.spec.js | tap-spec",
    "preversion": "npm test",
    "postversion": "git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/houtianze/resume-schema-ibic.git"
  },
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/houtianze/resume-schema-ibic/issues"
  },
  "files": [
    "resume.json",
    "schema.json",
    "validator.js"
  ],
  "dependencies": {
    "z-schema": "^4.2.2"
  },
  "devDependencies": {
    "tap-spec": "^5.0.0",
    "tape": "^4.13.2"
  },
  "engines": {
    "node": ">=10"
  }
}
